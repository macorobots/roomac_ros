version: "3.9"
services:
  roomac:
    image: roomac-laptop:latest
    build:
      context: ../
      dockerfile: docker/Dockerfile.laptop

    volumes:
      - roomac_data:/home/roomac/roomac_data
      - type: bind
        source: /dev
        target: /dev
    network_mode: host
    privileged: true

volumes:
  roomac_data:
    external: true
